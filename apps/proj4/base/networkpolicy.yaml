apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: proj4-app-allow-ingress
spec:
  podSelector:
    matchLabels:
      app: proj4-app
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 5000
