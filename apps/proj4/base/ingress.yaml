apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: proj4-app
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: proj4-app
                port:
                  number: 80
